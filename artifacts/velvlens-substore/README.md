# VelvLens Sub-Store evidence

This directory contains generated verification evidence for the standalone
Windows package. It must not contain subscription URLs, tokens, credentials, or
decoded user data. Fixture checks use `tests/fixtures/substore-vless.txt` only.

The final report is generated by `scripts/write-final-report.mjs` after the
Windows installer and portable package have been built and verified.
